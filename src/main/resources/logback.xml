<configuration>
    <!-- 日常运行日志（INFO 及以上级别） -->
    <appender name="INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${application.home:-.}/logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${application.home:-.}/logs/application-log-%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%date{yyyy-MM-dd HH:mm:ss ZZZZ} [%level] from [%logger] in %thread - %message%n%xException</pattern>
        </encoder>
        <!-- 允许 DEBUG+INFO+WARN+ERROR -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <!-- 错误日志（仅 ERROR 级别） -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${application.home:-.}/logs/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${application.home:-.}/logs/error-log-%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>365</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%date{yyyy-MM-dd HH:mm:ss ZZZZ} [%logger] in %thread - %message%n%xException</pattern>
        </encoder>
        <!-- 严格过滤仅 ERROR 级别 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>
    <!-- 控制台输出，方便开发调试 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{MM-dd HH:mm:ss ZZZZ} %level [%thread] - %message%n%xException{10}</pattern>
        </encoder>
    </appender>
    <!-- 客户端封包日志 -->
    <appender name="CLIENT_PACKET_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${application.home:-.}/logs/client_packet.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${application.home:-.}/logs/client_packet-log-%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>[CLIENT] %date{yyyy-MM-dd HH:mm:ss} [%level] - %message%n</pattern>
        </encoder>
    </appender>

    <!-- 服务端端封包日志 -->
    <appender name="SERVER_PACKET_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${application.home:-.}/logs/server_packet.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${application.home:-.}/logs/server_packet-log-%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>[SERVER] %date{yyyy-MM-dd HH:mm:ss} [%level] - %message%n</pattern>
        </encoder>
    </appender>

    <!-- 客户端封包日志 -->
    <appender name="ACPW_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${application.home:-.}/logs/acpw.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${application.home:-.}/logs/acpw-log-%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>[ACPW] %date{yyyy-MM-dd HH:mm:ss} [%level] - %message%n</pattern>
        </encoder>
    </appender>


    <!-- 异步追加到文件 -->
    <appender name="ASYNC_INFO_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="INFO_FILE"/>
    </appender>
    <appender name="ASYNC_ERROR_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ERROR_FILE"/>
    </appender>
    <appender name="ASYNC_CLIENT_PACKET_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CLIENT_PACKET_FILE"/>
    </appender>

    <appender name="ASYNC_SERVER_PACKET_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="SERVER_PACKET_FILE"/>
    </appender>

    <!-- 单一根 Logger 配置 -->
    <root level="INFO">
        <!-- 控制台输出 -->
        <appender-ref ref="STDOUT"/>
        <!-- 异步文件输出 -->
        <appender-ref ref="ASYNC_INFO_FILE"/>
        <appender-ref ref="ASYNC_ERROR_FILE"/>
    </root>


    <!-- 新增 Logger 配置 -->
    <logger name="CLIENT_PACKET" level="DEBUG" additivity="false">
        <appender-ref ref="ASYNC_CLIENT_PACKET_FILE"/>
    </logger>

    <logger name="SERVER_PACKET" level="DEBUG" additivity="false">
        <appender-ref ref="ASYNC_SERVER_PACKET_FILE"/>
    </logger>

    <logger name="ACPW" level="DEBUG" additivity="false">
        <appender-ref ref="ACPW_FILE"/>
    </logger>

    <!-- 自定义 Logger（关闭 additivity 避免日志传递到 root） -->
    <logger name="handling" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>
    <logger name="client" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>
    <logger name="KinMS" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>
    <logger name="com.github.mrzhqiang.maplestory" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>
    <logger name="gui" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>
    <logger name="server" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>
    <logger name="scripting" level="DEBUG" additivity="false">
        <appender-ref ref="INFO_FILE"/>
    </logger>

    <!-- 其他 TRACE 级别配置（如 SQL 日志） -->
    <logger name="io.ebean.SQL" level="TRACE"/>
    <logger name="io.ebean.TXN" level="TRACE"/>
    <logger name="io.ebean.SUM" level="TRACE"/>

    <!-- L2 logging -->
    <logger name="io.ebean.cache.QUERY" level="TRACE"/>
    <logger name="io.ebean.cache.BEAN" level="TRACE"/>
    <logger name="io.ebean.cache.COLL" level="TRACE"/>
    <logger name="io.ebean.cache.NATKEY" level="TRACE"/>

    <!-- Testing with Docker containers -->
    <logger name="io.ebean.docker" level="TRACE"/>

</configuration>
